<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">
<!--The
mapping document is an XML document having <hibernate-mapping> as the
    root element which contains two <class> elements corresponding to each class-->
<hibernate-mapping>
  <!--The
  <class> elements are used to define specific mappings from a Java
        classes to the database tables. The Java class name is specified using
        the name attribute of the class element and the database table name
        is specified using the table attribute-->
  <class name="company.entity.singleentity.Employee" table="EMPLOYEE">
    <!--The
    <meta> element is optional element and can be used to create the class description-->
    <meta attribute="class-description"> This class contains the employee detail. </meta>
    <!--The
    <id> element maps the unique ID attribute in class to the 
            primary key of the database table. The name attribute of the id
            element refers to the property in the class and the column
            attribute refers to the column in the database table. 
            The type attribute holds the hibernate mapping type, 
            this mapping types will convert from Java to SQL data type.-->
    <id column="id" name="id" type="int">
      <!--The
      <generator> element within the id element is used to
                automatically generate the primary key values.
                Set the class attribute of the generator element is set to native
                to let hibernate pick up either identity, sequence or hilo
                algorithm to create primary key depending upon the capabilities
                of the underlying database.-->
      <generator class="native" />
    </id>
    <!--The
    <property> element is used to map a Java class property to a column
            in the database table. The name attribute of the element refers to
            the property in the class and the column attribute refers to the
            column in the database table. The type attribute holds the hibernate
            mapping type, this mapping types will convert from Java to SQL data type.-->
    <property column="first_name" name="firstName" type="string" />
    <property column="last_name" name="lastName" type="string" />
    <property column="salary" name="salary" type="int" />
  </class>
</hibernate-mapping>